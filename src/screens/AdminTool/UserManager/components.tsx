import { Layout, Space } from "antd"
import styled from "styled-components"

export const ButtonLeftWrapper = styled(Layout)`
  display: flex;
  flex-grow: 0;
  flex-direction: row;
`
export const AvatarWrapper = styled(Space)`
  width: 100%;
  justify-content: center;
  align-items: center;
`
